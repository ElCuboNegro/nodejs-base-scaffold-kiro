# Development-only Python requirements
# These are additional tools for development that aren't needed in production

# Include base requirements
-r requirements.txt

# Development and debugging tools
ipython>=8.0.0,<9.0.0      # Enhanced Python REPL
ipdb>=0.13.0,<1.0.0        # Python debugger

# Code quality tools
mypy>=1.0.0,<2.0.0         # Static type checker
pylint>=2.17.0,<3.0.0      # Advanced Python linter
isort>=5.12.0,<6.0.0       # Import sorter

# Testing and coverage
pytest-xdist>=3.0.0,<4.0.0    # Parallel test execution
pytest-mock>=3.10.0,<4.0.0    # Mocking for tests
coverage>=7.0.0,<8.0.0        # Coverage reporting

# Documentation development
sphinx-autobuild>=2021.3.14   # Auto-rebuilding docs
sphinx-rtd-theme>=1.3.0       # ReadTheDocs theme

# Pre-commit hooks development
pre-commit-hooks>=4.4.0       # Additional pre-commit hooks
tornado>=6.3.2 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=1.26.19 # not directly required, pinned by Snyk to avoid a vulnerability
requests>=2.32.2 # not directly required, pinned by Snyk to avoid a vulnerability
